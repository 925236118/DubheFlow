feat: Planner 工作流生成 + Interpreter 工作流执行器 (Phase 4-5)

Phase 4 — Planner(规划层):
- planner.ts: 从原语注册表自动生成系统提示
  (每个原语 schema + 描述 + 可用 capability + 全局变量 + 数据流语法)
- generateSpec(): 调模型生成 spec JSON → 容错解析(去 markdown/提取 {})
  → 静态校验 → 不过则带错误重试(max_retries)
- 生成成功后存入 DB(workflow + workflow_revision 草稿态)
- IPC: planner:generate

Phase 5 — Interpreter(执行层):
- interpreter.ts: 读 spec → 拓扑排序(Kahn) → 逐节点执行
  - 数据流解析:每步执行前 deepInterpolate({{ }} + $ref)
  - llm_generate: 调 provider 流式生成,产出 text/patch
  - verify: 规则优先(退出码/stderr grep/文件存在/空输出)
  - collect/require/approve/assemble/split: 基础桩
- 每步写 step_run 到 DB,模型调用写 model_call
- 运行事件流式推送(step_start/delta/done/failed/verify/run_done)
- 支持取消(AbortController)
- IPC: interpreter:run(流式) + interpreter:cancel
- preload 暴露 spec.generate / spec.run API
